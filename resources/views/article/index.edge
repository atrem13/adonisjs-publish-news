@layout('layouts.main')

@section('page')
<ul>
  @each(article in articles)
  <a href="{{ route('article.show', {id: article.id})}}">
    <h3>{{ article.title }}</h3>
  </a>
  @endeach
</ul>
@endsection